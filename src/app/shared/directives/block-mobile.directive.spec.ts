import { BlockMobileDirective } from './block-mobile.directive';

describe('BlockMobileDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockMobileDirective();
    expect(directive).toBeTruthy();
  });
});
