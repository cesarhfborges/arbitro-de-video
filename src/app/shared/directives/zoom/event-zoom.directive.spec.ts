import { EventZoomDirective } from './event-zoom.directive';

describe('EventZoomDirective', () => {
  it('should create an instance', () => {
    const directive = new EventZoomDirective();
    expect(directive).toBeTruthy();
  });
});
